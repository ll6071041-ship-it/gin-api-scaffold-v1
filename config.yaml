app:
  name: "gin-api-scaffold-v1"
  port: 8080

mysql:
  user: "root"
  password: "123456"           # 👈 别人用的时候填他们自己的，这里留空
  host: "127.0.0.1"
  port: 3306
  dbname: "gin_project"     # 👈 改个通用的名字，别叫 bubble 了

redis:
  host: "192.168.64.128"      # 👈 默认填本地回环
  port: 6379
  password: ""
  db: 0

# 新增日志配置
log:
  level: "debug"
  filename: "./logs/my-app.log" # 建议放在 logs 文件夹下
  max_size: 10      # 每个文件 10MB
  max_backups: 5    # 保留 5 个旧文件
  max_age: 30       # 保留 30 天